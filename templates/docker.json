{
  "variables": {
  },
  "builders": [
    {
      "type": "docker",
      "image": "ubuntu",
      "commit": true,
      "run_command": ["-d", "-i", "-t", "{{.Image}}", "/bin/bash"]
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "script": "provisioners/pre.sh"
    },
    {
      "type": "puppet-masterless",
      "puppet_bin_dir": "/opt/puppetlabs/bin/",
      "extra_arguments": "--debug -e 'include epel'",
      "staging_directory": "/tmp/packer-swaggy-c",
      "manifest_file": "provisioners/main.pp",
      "module_paths": [
        "modules",
        "modules-extra"
      ]
    },
    {
      "type": "shell",
      "script": "provisioners/post.sh"
    }
  ],
  "post-processors": [
    {
      "type": "docker-tag",
      "repository": "cliffano/swaggy-c",
      "tag": "latest",
      "only": ["docker"]
    }
  ]
}
